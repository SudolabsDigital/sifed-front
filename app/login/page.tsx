import LoginHub from "@/components/auth/login-hub";

export const metadata = {
  title: "Hub de Servicios | SIFED",
  description: "Acceso centralizado a servicios académicos y administrativos.",
};

export default function LoginPage() {
  return <LoginHub />;
}